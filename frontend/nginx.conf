server { listen 8080; server_tokens off; root /usr/share/nginx/html; location /api/ { proxy_pass http://api:4000; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; } location / { try_files $uri /index.html; } }
